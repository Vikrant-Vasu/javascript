const name = "Vikrant";
const marks = 95;

console.log(name + " "+ marks)
//better way to add strings is string interpolation

console.log(`My name is ${name} and my marks are ${marks}`);

//we have other method to declare string

const gameName = new String('VikrantVasu')
console.log(gameName.length);
console.log(gameName.toUpperCase());
console.log(gameName);
